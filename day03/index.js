const menu = window.prompt("점심 뭐먹음");

window.console.log(`점심으로 ${menu}를 먹었다니 맛있었겠군`);


const baes=Number(prompt("밑변"));
const height=Number(prompt("높이"));
console.log(`정삼각형의 넓이는 ${base * height * 0.5}`)


const won = Number(prompt("원화입력"));
const rate=0.113;
console.log(`엔화: ${원화 * rate}`);